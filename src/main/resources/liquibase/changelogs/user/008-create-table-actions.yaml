databaseChangeLog:
  - changeSet:
      id: user-008
      author: Jbbcch
      preConditions:
        onFail: MARK_RAN
        not:
          - tableExists:
              tableName: actions
              schemaName: user_schema
      changes:
        - createTable:
            schemaName: user_schema
            tableName: actions
            columns:
              - column:
                  name: id
                  type: BIGINT
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: action
                  type: VARCHAR(50)
                  constraints:
                    nullable: false