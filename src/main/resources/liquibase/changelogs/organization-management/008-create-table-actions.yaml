databaseChangeLog:
  - changeSet:
      id: organization-008
      author: Jbbcch
      preConditions:
        onFail: MARK_RAN
        not:
          - tableExists:
              tableName: actions
              schemaName: organization_management
      changes:
        - createTable:
            schemaName: organization_management
            tableName: actions
            columns:
              - column:
                  name: id
                  type: BIGINT
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: action
                  type: VARCHAR(50)
                  constraints:
                    nullable: false