databaseChangeLog:
  - changeSet:
      id: organization-004
      author: Jbbcch
      preConditions:
        onFail: MARK_RAN
        not:
          - tableExists:
              tableName: permissions
              schemaName: organization_management
      changes:
        - createTable:
            schemaName: organization_management
            tableName: permissions
            columns:
              - column:
                  name: id
                  type: BIGINT
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: action
                  type: VARCHAR(50)
                  constraints:
                    nullable: false
              - column:
                  name: resource
                  type: VARCHAR(50)
                  constraints:
                    nullable: false