databaseChangeLog:
  - changeSet:
      id: organization-009
      author: Jbbcch
      preConditions:
        onFail: MARK_RAN
        not:
          - tableExists:
              tableName: resources
              schemaName: organization_management
      changes:
        - createTable:
            schemaName: organization_management
            tableName: resources
            columns:
              - column:
                  name: id
                  type: BIGINT
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: resource
                  type: VARCHAR(50)
                  constraints:
                    nullable: false